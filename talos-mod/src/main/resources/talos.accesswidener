accessWidener v2 official

# GuiGraphicsExtractor.guiRenderState is already public in the 26.2 compile
# namespace through Fabric API's transitive access widener, so Talos no longer needs
# to widen it itself.
